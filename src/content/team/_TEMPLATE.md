---
# This file is a template — Astro ignores files starting with `_`.
# To add a person:
#   1. Copy this file to a new name, e.g. `dennis-lee.md`.
#   2. Fill the frontmatter below.
#   3. (Optional) Drop a square photo into `/public/team/<slug>.jpg`
#      and set `photo: "/team/<slug>.jpg"`.
#   4. Body text below the frontmatter is the bio (markdown allowed).
#   5. Commit + push. The /about page picks it up on next build.
#
# To remove someone temporarily, set `active: false` (keeps the file).
# To remove permanently, delete the file.
# `order` controls the display order on /about — lower = earlier.

name: "Full Name"
role: "Title at KKT"
photo: "/team/template.jpg"  # delete this line if no photo yet
order: 50
active: true
links:
  email: "name@kittykat.tech"
  linkedin: "https://www.linkedin.com/in/handle/"
---

One paragraph bio. Plain English, no marketing fluff. What this person
does at KKT, where they came from, what they care about. Two to four
sentences is plenty.
