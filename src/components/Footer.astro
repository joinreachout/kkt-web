---
// Footer — legal name "Kitty Kat Technologies" appears here per V2 §3.1.
import CatMark from './CatMark.astro';
import { useStrings } from '~/i18n/strings';
import { localizePath, toLocale } from '~/i18n/config';
const locale = toLocale(Astro.currentLocale);
const t = useStrings(Astro.currentLocale);
const L = (p: string) => localizePath(p, locale);
const year = new Date().getFullYear();
---

<footer class="site-footer">
  <div class="container footer-inner">
    <div class="footer-brand">
      <div class="footer-wordmark-row">
        <CatMark size={28} ariaLabel="Kitty Kat Technologies" />
        <span class="footer-wordmark">KKT</span>
      </div>
      <p class="footer-tag">{t.footer.tag}</p>
    </div>

    <div class="footer-cols">
      <div>
        <div class="footer-col-label">{t.footer.industries}</div>
        <ul>
          <li><a href={L('/industries/retail')}>{t.nav.retail}</a></li>
          <li><a href={L('/industries/fuel-retail')}>{t.nav.fuel}</a></li>
        </ul>
      </div>

      <div>
        <div class="footer-col-label">{t.footer.work}</div>
        <ul>
          <li><a href={L('/case-studies/')}>{t.nav.cases}</a></li>
          <li><a href={L('/solutions')}>{t.nav.solutions}</a></li>
        </ul>
      </div>

      <div>
        <div class="footer-col-label">{t.footer.company}</div>
        <ul>
          <li><a href={L('/about')}>{t.nav.about}</a></li>
          <li><a href={L('/insights')}>{t.nav.insights}</a></li>
          <li><a href={L('/contact')}>{t.footer.contact}</a></li>
          <li><button type="button" class="footer-link-button" data-advisor-open>{t.actions.advisor}</button></li>
        </ul>
      </div>
    </div>
  </div>

  <div class="container footer-legal">
    <p>{t.footer.legal(year)}</p>
    <p>
      <a href="mailto:hello@kittykat.tech">hello@kittykat.tech</a>
      <span class="sep">&middot;</span>
      <a href="tel:+37255553621">+372 5555 3621</a>
      <span class="sep">&middot;</span>
      <a href="https://www.linkedin.com/company/kittykattech/" rel="noopener" target="_blank">LinkedIn</a>
    </p>
  </div>
</footer>

<style>
  .site-footer {
    background: var(--surface-alt);
    border-top: 1px solid var(--border);
    padding-top: clamp(48px, 7vh, 80px);
    padding-bottom: 28px;
    margin-top: 48px;
  }
  .footer-inner {
    display: grid;
    grid-template-columns: 1fr;
    gap: 40px;
    padding-bottom: 40px;
  }
  @media (min-width: 720px) {
    .footer-inner {
      grid-template-columns: 1fr 2fr;
      gap: 64px;
    }
  }

  .footer-wordmark-row {
    display: flex;
    align-items: center;
    gap: 10px;
    color: var(--text-primary);
    margin-bottom: 8px;
  }
  .footer-wordmark {
    font-size: 13px;
    font-weight: 600;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--text-primary);
  }
  .footer-tag {
    font-family: var(--font-display);
    font-style: normal;
    color: var(--text-secondary);
    font-size: 16px;
    line-height: 1.4;
    max-width: 26ch;
  }

  .footer-cols {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
    gap: 32px;
  }
  .footer-col-label {
    font-size: 11px;
    color: var(--text-muted);
    font-weight: 600;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    margin-bottom: 12px;
  }
  .footer-cols ul {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .footer-cols a {
    font-size: 14px;
    color: var(--text-secondary);
    text-decoration: none;
    transition: color .15s ease;
  }
  .footer-cols a:hover {
    color: var(--text-primary);
    text-decoration: underline;
    text-underline-offset: 3px;
    text-decoration-color: var(--border);
  }
  .footer-link-button {
    background: transparent;
    border: none;
    padding: 0;
    font-family: inherit;
    font-size: 14px;
    color: var(--text-secondary);
    cursor: pointer;
    text-align: left;
    transition: color .15s ease;
  }
  .footer-link-button:hover {
    color: var(--text-primary);
    text-decoration: underline;
    text-underline-offset: 3px;
    text-decoration-color: var(--accent);
  }

  .footer-legal {
    border-top: 1px solid var(--border);
    padding-top: 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 12px;
  }
  .footer-legal p {
    font-size: 12px;
    color: var(--text-muted);
    margin: 0;
  }
  .footer-legal a {
    font-size: 12px;
    color: var(--text-muted);
    text-decoration: none;
  }
  .footer-legal a:hover {
    color: var(--text-primary);
  }
  .footer-legal .sep {
    margin: 0 8px;
    color: var(--text-muted);
  }
</style>
