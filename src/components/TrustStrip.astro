---
// Trust strip. Three deployable variants per V2 §3.3 / hero spec.
// `variant` prop selects the line. Phase 1 currently runs C (full topline,
// pending Alfa Oil consent — see WORK/briefs/alfa-oil-content-brief.md).
// On consent change, swap the prop in index.astro — no rebuild required.
type Variant = 'A' | 'B' | 'C';
interface Props {
  variant?: Variant;
}
const { variant = 'C' } = Astro.props as Props;

const logos = [
  { src: '/logos/alfa-oil.png', alt: 'Alfa Oil' },
  { src: '/logos/click-and-grow.png', alt: 'Click & Grow' },
  { src: '/logos/gte.png', alt: 'GTE' },
  { src: '/logos/kricon.png', alt: 'Kricon' },
  { src: '/logos/csc-telecom.png', alt: 'CSC Telecom' },
];
---

<div class={`trust-strip variant-${variant}`}>
  {variant === 'B' && (
    <p class="trust-line">
      Live with <strong>Alfa Oil</strong> — fuel network, CE region.
    </p>
  )}

  {variant === 'C' && (
    <p class="trust-line">
      Live with <strong>Alfa Oil</strong> — $500M revenue, 600+ stations, Optimus operating.
    </p>
  )}

  <div class="logo-strip">
    {logos.map((l) => (
      <div class="logo-chip">
        <img class="logo" src={l.src} alt={l.alt} loading="lazy" />
      </div>
    ))}
  </div>

  <p class="trust-tail">Tallinn, EU</p>
</div>

<style>
  .trust-strip {
    margin-top: 28px;
    padding-top: 24px;
    border-top: 1px solid var(--border);
    max-width: 760px;
  }
  .trust-line {
    font-size: 14px;
    line-height: 1.7;
    color: var(--text-secondary);
    margin: 0;
  }
  .trust-line strong {
    color: var(--text-primary);
    font-weight: 600;
  }
  @media (min-width: 720px) {
    .trust-line { font-size: 15px; }
  }

  .logo-strip {
    display: flex;
    flex-wrap: wrap;
    align-items: stretch;
    gap: 12px;
    margin-top: 22px;
    margin-bottom: 18px;
  }
  .logo-chip {
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 4px;
    width: 132px;
    height: 88px;
    padding: 14px 18px;
    transition: border-color .18s ease;
  }
  .logo-chip:hover { border-color: var(--border-hover); }
  .logo {
    max-width: 100%;
    max-height: 100%;
    width: auto;
    height: auto;
    object-fit: contain;
    display: block;
  }
  @media (max-width: 720px) {
    .logo-chip { width: 110px; height: 76px; padding: 12px 14px; }
  }

  .trust-tail {
    font-size: 12px;
    color: var(--text-muted);
    letter-spacing: 0.14em;
    text-transform: uppercase;
    font-weight: 600;
    margin: 0;
  }
</style>
